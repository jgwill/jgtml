# GitHub Pages/Jekyll configuration for jgtml documentation site
# Minimal exclusions to ensure GitHub Pages build succeeds

exclude:
  # Large data directories
  - jgtml_refresh
  - models
  - book
  
  # Tool and editor directories
  - .cursor
  - .vscode
  - .jgt
  - .mia
  
  # Build directories
  - dist
  - build
