# Overview

`jgtml` provides tools for analyzing trading signals, generating datasets, and managing trading operations. Core features include multi-timeframe Alligator analysis, FDB signal validation, and matrix generation for machine learning. The platform integrates tightly with `jgtpy` for data acquisition and exposes several CLI utilities for workflow automation.
